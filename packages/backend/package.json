{
  "name": "@paperland/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@paperland/shared": "workspace:*",
    "fastify": "*",
    "@fastify/cors": "*",
    "@fastify/static": "*",
    "drizzle-orm": "*",
    "js-yaml": "^4.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/js-yaml": "*",
    "drizzle-kit": "*"
  }
}

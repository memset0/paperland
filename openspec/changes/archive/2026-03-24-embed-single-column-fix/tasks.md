## 1. Implementation

- [x] 1.1 In PaperDetail.vue, import `useEmbedMode` and create a computed `showSplitView` that is `isWide && !isEmbed`, then replace `isWide` with `showSplitView` in template conditionals for the split/single layout switch.

## 2. Documentation

- [x] 2.1 Update `docs/frontend-architecture.md` to note that embed mode forces single-column layout.

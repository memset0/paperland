## 1. Implementation

- [x] 1.1 Implement callCodex in qa_service.ts: shell command execution, explicit env inheritance (process.env), timeout with kill, shell quoting
- [x] 1.2 Add codex model example to config.yml with shell and timeout fields
- [x] 1.3 Update config Zod schema to support shell and timeout fields on model definitions, add 'codex' type

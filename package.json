{
  "name": "paperland",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run packages/backend/src/index.ts & bun run --filter '@paperland/frontend' dev",
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '@paperland/backend' test"
  }
}
